using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//TODO WIP, will activate own effects like a skill and be recastable x times for y seconds activating different effects
public class SkillRecastable : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
